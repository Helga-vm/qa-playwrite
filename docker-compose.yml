version: "3"

services:
  auto-tests:
    container_name: playwright-tests_2
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      CI: true
      BASE_URL: $BASE_URL
      HTTP_CREDENTIALS_USERNAME: $HTTP_CREDENTIALS_USERNAME
      HTTP_CREDENTIALS_PASSWORD: $HTTP_CREDENTIALS_PASSWORD